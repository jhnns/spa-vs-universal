import cxs from "cxs";
import { px } from "../../styles/scales";

export const logoImg = cxs({
    display: "block",
    width: px(20),
});

// module.exports = [[module.id, cxs.getCss()]];
// cxs.reset();
