import { px } from "../../styles/scales";
import { white, black } from "../../styles/colors";

export const sheetPadding = px(13);

export default {
    color: black(),
    backgroundColor: white(),
    padding: sheetPadding,
};
